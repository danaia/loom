# Loom 0.1.8

Loom 0.1.8 personalizes the Baseline Controls window for each newly created
project and makes release packaging lighter.

## Highlights

- `loom new network` titles the Controls window `Loom network — Controls`.
- Release packaging caches Rust build artifacts and does not rebuild examples.
